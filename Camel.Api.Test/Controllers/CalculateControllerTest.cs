using NUnit.Framework;

namespace Lepecki.Playground.Camel.Api.Test.Controllers
{
    [TestFixture]
    public class CalculateControllerTest
    {
        [Test]
        public void Test()
        {
            // ICalc calcMock = new Mock<ICalc>().Object;
            // var controller = new CalculateController(calcMock);
            // var result = controller.Calculate(null);
            // result.Result.ExecuteResult();
        }
    }
}